<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jakt.aiplatform.common.dal.mapper.SysNoticeMapper">

    <sql id="selectColumns">
        notice_id, notice_title, notice_type, notice_content, status, remark, create_by, update_by, create_time, update_time
    </sql>

    <sql id="queryConditions">
        <where>
            <if test="noticeId != null">
                AND notice_id = #{noticeId}
            </if>
            <if test="noticeTitle != null and noticeTitle != ''">
                AND notice_title = #{noticeTitle}
            </if>
            <if test="noticeType != null and noticeType != ''">
                AND notice_type = #{noticeType}
            </if>
            <if test="noticeContent != null and noticeContent != ''">
                AND notice_content = #{noticeContent}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
            <if test="remark != null and remark != ''">
                AND remark = #{remark}
            </if>
            <if test="createTimeBegin != null">
                AND create_time &gt;= #{createTimeBegin}
            </if>
            <if test="createTimeEnd != null">
                AND create_time &lt;= #{createTimeEnd}
            </if>
            <if test="updateTimeBegin != null">
                AND update_time &gt;= #{updateTimeBegin}
            </if>
            <if test="updateTimeEnd != null">
                AND update_time &lt;= #{updateTimeEnd}
            </if>
        </where>
    </sql>

    <select id="selectById" resultType="com.jakt.aiplatform.common.dal.dataobject.SysNoticeDO">
        SELECT <include refid="selectColumns"/>
        FROM sys_notice
        WHERE notice_id = #{id}
    </select>

    <select id="selectPage" resultType="com.jakt.aiplatform.common.dal.dataobject.SysNoticeDO">
        SELECT <include refid="selectColumns"/>
        FROM sys_notice
        <include refid="queryConditions"/>
        ORDER BY notice_id DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <select id="selectList" resultType="com.jakt.aiplatform.common.dal.dataobject.SysNoticeDO">
        SELECT <include refid="selectColumns"/>
        FROM sys_notice
        <include refid="queryConditions"/>
        ORDER BY notice_id DESC
    </select>

    <select id="countByQuery" resultType="long">
        SELECT COUNT(*)
        FROM sys_notice
        <include refid="queryConditions"/>
    </select>

    <insert id="insert" parameterType="com.jakt.aiplatform.common.dal.dataobject.SysNoticeDO"
            useGeneratedKeys="true" keyProperty="noticeId">
        INSERT INTO sys_notice (notice_title, notice_type, notice_content, status, remark, create_by, update_by)
        VALUES (#{noticeTitle}, #{noticeType}, #{noticeContent}, #{status}, #{remark}, #{createBy}, #{updateBy})
    </insert>

    <update id="update" parameterType="com.jakt.aiplatform.common.dal.dataobject.SysNoticeDO">
        UPDATE sys_notice
        SET notice_title = #{noticeTitle},
            notice_type = #{noticeType},
            notice_content = #{noticeContent},
            status = #{status},
            remark = #{remark}
        WHERE notice_id = #{noticeId}
    </update>

    <update id="updateByCondition" parameterType="com.jakt.aiplatform.common.dal.dataobject.SysNoticeDO">
        <if test="noticeTitle != null or noticeType != null or noticeContent != null or status != null or remark != null">
            UPDATE sys_notice
        <set>
            <if test="noticeTitle != null">
                notice_title = #{noticeTitle},
            </if>
            <if test="noticeType != null">
                notice_type = #{noticeType},
            </if>
            <if test="noticeContent != null">
                notice_content = #{noticeContent},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="remark != null">
                remark = #{remark},
            </if>
        </set>
            WHERE notice_id = #{noticeId}
        </if>
    </update>

    <delete id="deleteById">
        DELETE FROM sys_notice WHERE notice_id = #{id}
    </delete>

    <delete id="deleteByIds">
        DELETE FROM sys_notice WHERE notice_id IN
        <foreach collection="array" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>
