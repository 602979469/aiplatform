<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jakt.aiplatform.common.dal.mapper.AiChatMessageMapper">

    <sql id="selectColumns">
        message_id, session_id, user_id, role, content, status, create_time, update_time
    </sql>

    <select id="selectById" resultType="com.jakt.aiplatform.common.dal.dataobject.AiChatMessageDO">
        SELECT <include refid="selectColumns"/>
        FROM ai_chat_message
        WHERE message_id = #{id}
        AND del_flag = '0'
    </select>

    <insert id="insert" parameterType="com.jakt.aiplatform.common.dal.dataobject.AiChatMessageDO"
            useGeneratedKeys="true" keyProperty="messageId">
        INSERT INTO ai_chat_message (session_id, user_id, role, content, status, create_time, update_time)
        VALUES (#{sessionId}, #{userId}, #{role}, #{content}, #{status}, NOW(), NOW())
    </insert>

    <update id="updateStatusById">
        UPDATE ai_chat_message
        SET status = #{status}
        WHERE message_id = #{messageId} AND del_flag = '0'
    </update>

    <update id="deleteBySessionId">
        UPDATE ai_chat_message
        SET del_flag = '2'
        WHERE session_id = #{sessionId} AND del_flag = '0'
    </update>

    <select id="selectBySessionAsc" resultType="com.jakt.aiplatform.common.dal.dataobject.AiChatMessageDO">
        SELECT <include refid="selectColumns"/>
        FROM ai_chat_message
        WHERE session_id = #{sessionId} AND del_flag = '0'
        ORDER BY message_id ASC
    </select>
</mapper>
