<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jakt.aiplatform.common.dal.mapper.AuthUserMapper">

    <sql id="selectColumns">
        user_id, username, nickname, password, email, avatar, status, remark, create_time, update_time
    </sql>

    <sql id="queryConditions">
        <where>
            <if test="userId != null">
                AND user_id = #{userId}
            </if>
            <if test="username != null and username != ''">
                AND username = #{username}
            </if>
            <if test="nickname != null and nickname != ''">
                AND nickname = #{nickname}
            </if>
            <if test="email != null and email != ''">
                AND email = #{email}
            </if>
            <if test="avatar != null and avatar != ''">
                AND avatar = #{avatar}
            </if>
            <if test="status != null">
                AND status = #{status.code}
            </if>
            <if test="remark != null and remark != ''">
                AND remark = #{remark}
            </if>
            AND del_flag = '0'
            <if test="createTimeBegin != null">
                AND create_time &gt;= #{createTimeBegin}
            </if>
            <if test="createTimeEnd != null">
                AND create_time &lt;= #{createTimeEnd}
            </if>
            <if test="updateTimeBegin != null">
                AND update_time &gt;= #{updateTimeBegin}
            </if>
            <if test="updateTimeEnd != null">
                AND update_time &lt;= #{updateTimeEnd}
            </if>
        </where>
    </sql>

    <select id="selectById" resultType="com.jakt.aiplatform.common.dal.dataobject.AuthUserDO">
        SELECT <include refid="selectColumns"/>
        FROM auth_user
        WHERE user_id = #{id}
        AND del_flag = '0'
    </select>

    <select id="selectByUsername" resultType="com.jakt.aiplatform.common.dal.dataobject.AuthUserDO">
        SELECT <include refid="selectColumns"/>
        FROM auth_user
        WHERE username = #{username}
        AND del_flag = '0'
    </select>

    <select id="selectPage" resultType="com.jakt.aiplatform.common.dal.dataobject.AuthUserDO">
        SELECT <include refid="selectColumns"/>
        FROM auth_user
        <include refid="queryConditions"/>
        ORDER BY user_id DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <select id="selectList" resultType="com.jakt.aiplatform.common.dal.dataobject.AuthUserDO">
        SELECT <include refid="selectColumns"/>
        FROM auth_user
        <include refid="queryConditions"/>
        ORDER BY user_id DESC
    </select>

    <select id="countByQuery" resultType="long">
        SELECT COUNT(*)
        FROM auth_user
        <include refid="queryConditions"/>
    </select>

    <insert id="insert" parameterType="com.jakt.aiplatform.common.dal.dataobject.AuthUserDO"
            useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO auth_user (username, nickname, password, email, avatar, status, remark)
        VALUES (#{username}, #{nickname}, #{password}, #{email}, #{avatar}, #{status}, #{remark})
    </insert>

    <update id="update" parameterType="com.jakt.aiplatform.common.dal.dataobject.AuthUserDO">
        UPDATE auth_user
        SET username = #{username},
            nickname = #{nickname},
            password = #{password},
            email = #{email},
            avatar = #{avatar},
            status = #{status},
            remark = #{remark}
        WHERE user_id = #{userId}
        AND del_flag = '0'
    </update>

    <update id="updateByCondition" parameterType="com.jakt.aiplatform.common.dal.dataobject.AuthUserDO">
        <if test="username != null or nickname != null or password != null or email != null or avatar != null or status != null or remark != null">
            UPDATE auth_user
        <set>
            <if test="username != null">
                username = #{username},
            </if>
            <if test="nickname != null">
                nickname = #{nickname},
            </if>
            <if test="password != null">
                password = #{password},
            </if>
            <if test="email != null">
                email = #{email},
            </if>
            <if test="avatar != null">
                avatar = #{avatar},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="remark != null">
                remark = #{remark},
            </if>
        </set>
            WHERE user_id = #{userId}
            AND del_flag = '0'
        </if>
    </update>

    <update id="deleteById" parameterType="com.jakt.aiplatform.common.dal.dataobject.AuthUserDO">
        UPDATE auth_user
        SET del_flag = '2'
        WHERE user_id = #{id} AND del_flag = '0'
    </update>
</mapper>
