<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jakt.aiplatform.common.dal.mapper.AiCapabilityMapper">

    <sql id="selectColumns">
        capability_id, scene_code, capability_code, capability_name, description, skill_rules, status, remark, create_time, update_time
    </sql>

    <select id="selectBySceneAndCode" resultType="com.jakt.aiplatform.common.dal.dataobject.AiCapabilityDO">
        SELECT <include refid="selectColumns"/>
        FROM sys_ai_capability
        WHERE scene_code = #{sceneCode}
          AND capability_code = #{capabilityCode}
          AND status = '0'
    </select>
</mapper>
